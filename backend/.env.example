PORT=4000
JWT_SECRET=change-this-to-a-long-random-string

# Optional: add a real Stripe secret key to accept live card payments.
# Without it, card checkout runs in simulated mode so you can still test the full flow.
STRIPE_SECRET_KEY=
